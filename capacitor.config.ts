import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wxz.maketen',
  appName: 'Make Ten',
  webDir: 'dist',
  backgroundColor: '#141824',
};

export default config;
